import styled from 'styled-components'

export const ContentButton = styled.div`
   display: flex;
   margin-right: .5rem;
   justify-content: end;
   button {
      background-color: #05BB38;
      border-radius: 5px;
      color: white;
      font-size: 1rem;
      padding: 1rem 2rem;
   }
   button:hover {
      cursor: pointer;
   }
   
`