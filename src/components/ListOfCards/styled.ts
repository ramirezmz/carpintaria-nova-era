import styled from 'styled-components'

export const Content = styled.div`
   background: none;
   display: flex;
   align-items: center;
   justify-content: center;
   flex-wrap: wrap;
   width: 80vw;
   padding: 2rem;
`